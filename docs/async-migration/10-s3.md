# PR #10 · 阶段 10：S3 切 aioboto3

## 目标

把 `boto3` 同步 S3 调用替换为 `aioboto3` 异步调用，去掉 PR #4 阶段引入的 `asyncio.to_thread` 桥接。

## 关键决策（与原计划偏差）

| 维度 | 原计划（10-s3.md 初版） | 实际执行（PR #10） |
|------|----------------------|------------------|
| 策略 | `S3Adapter` 改 async | **保留** sync `S3Adapter`（boto3），**新增** `AsyncS3Adapter`（aioboto3），共存到 PR #11 收尾 |
| 方法签名 | `UploadFile(file_obj, key) -> None` / `DownloadFile(key) -> bytes` | **保持与 sync 版完全一致**：`UploadFile(local_path, key) -> str` / `DownloadFile(key, local_path) -> bool` / `DeleteFile(key) -> bool`（仅 sync → async） |
| `AsyncFileService` 调用点 | 大量重写 | **4 处 `to_thread` 桥接删除即可**，其他不动 |
| `pool_runner._cleanup_expired_files` | 未规划 | **保持 sync 路径**，用 sync `S3Adapter` + sync `FileService`（与 PR #8 决策一致） |
| `startup._register_file_cleanup_job` | 未规划 | **保持 sync 路径**（同上） |
| `boto3` 处理 | 未明确 | **保留到 PR #11 收尾**统一删（与 PR #4/#8 决策一致） |
| `aioboto3` 版本 | 计划阶段未定 | 实际装 `aioboto3==15.5.0`（最新稳定），强制 `boto3>=1.40.61` |

## 改动文件清单

| 文件 | 类型 | 改动点 |
|------|------|--------|
| `requirements.txt` | 修改 | `boto3==1.40.39` → `boto3==1.40.61`；新增 `aioboto3==15.5.0` |
| `app/adapter/s3_adapter.py` | 修改 | **新增** `AsyncS3Adapter` 类（async 版 3 方法 + 私有 `_enabled` / `_client_kwargs` / `_build_url`）；**保留** `S3Adapter`（sync）不动 |
| `app/api/deps.py` | 修改 | **新增** `get_async_s3_adapter` 工厂（`lru_cache(maxsize=1)`，与 `get_s3_adapter` 并存）；`get_async_file_service` 注入 `get_async_s3_adapter()` |
| `app/service/file_service.py` | 修改 | `AsyncFileService.__init__` 参数 `s3_adapter: S3Adapter` → `s3_adapter: AsyncS3Adapter`；4 处 `await asyncio.to_thread(self._s3_adapter.X, ...)` 改为 `await self._s3_adapter.X(...)`；docstring 注释更新 |
| `tests/test_async_s3_adapter.py` | 新增 | 7 用例覆盖 3 方法 × {disabled, enabled}, 1 用例覆盖 endpoint_url 拼接 |
| `docs/CHANGELOG-ASYNC-MIGRATION.zh-CN.md` | 修改 | 追加 PR #10 进度 |
| `docs/async-migration/10-s3.md` | 修改 | **本文档**（改写为「共存 + 一致签名」版本） |

## 关键代码骨架

### `app/adapter/s3_adapter.py` 增量（保留 sync 版，新增 async 版）

```python
class AsyncS3Adapter:
    """S3 适配器（异步版本，aioboto3 驱动）。PR #10 引入。"""

    def __init__(self, settings: Settings, session=None) -> None:
        self._settings = settings
        self._session = session if session is not None else aioboto3.Session()

    def _enabled(self) -> bool:
        return bool(
            self._settings.s3_bucket_name
            and self._settings.s3_access_key_id
            and self._settings.s3_secret_access_key
        )

    def _client_kwargs(self) -> dict:
        return {
            "endpoint_url": self._settings.s3_endpoint_url or None,
            "aws_access_key_id": self._settings.s3_access_key_id,
            "aws_secret_access_key": self._settings.s3_secret_access_key,
            "region_name": self._settings.s3_region_name or None,
        }

    @staticmethod
    def _build_url(key: str, settings: Settings) -> str:
        if settings.s3_endpoint_url:
            endpoint = settings.s3_endpoint_url.rstrip("/")
            return f"{endpoint}/{settings.s3_bucket_name}/{key}"
        region = settings.s3_region_name or "us-east-1"
        return f"https://{settings.s3_bucket_name}.s3.{region}.amazonaws.com/{key}"

    async def UploadFile(self, local_path: str, key: str) -> str: ...
    async def DownloadFile(self, key: str, local_path: str) -> bool: ...
    async def DeleteFile(self, key: str) -> bool: ...
```

### `AsyncFileService` 4 处桥接移除（伪 diff）

```diff
@@ class AsyncFileService:
-    s3_adapter: S3Adapter,
+    s3_adapter: AsyncS3Adapter,
@@
-            s3_url = await asyncio.to_thread(
-                self._s3_adapter.UploadFile,
-                local_path=str(local_path),
-                key=object_key,
-            )
+            s3_url = await self._s3_adapter.UploadFile(
+                local_path=str(local_path),
+                key=object_key,
+            )
@@ DownloadFile
-            await asyncio.to_thread(
-                self._s3_adapter.DownloadFile,
-                key=file_record.s3_key,
-                local_path=str(local_path),
-            )
+            await self._s3_adapter.DownloadFile(
+                key=file_record.s3_key,
+                local_path=str(local_path),
+            )
@@ SoftDeleteFile
-        await asyncio.to_thread(self._s3_adapter.DeleteFile, key=file_record.s3_key)
+        await self._s3_adapter.DeleteFile(key=file_record.s3_key)
@@ CleanupExpiredFiles
-                    await asyncio.to_thread(self._s3_adapter.DeleteFile, key=file_record.s3_key)
+                    await self._s3_adapter.DeleteFile(key=file_record.s3_key)
```

## 风险点

1. **aioboto3 client 必须在 `async with` 上下文中使用**（`__aenter__` / `__aexit__`）——已在骨架中处理；每次调用都新建上下文，不缓存 client
2. **aioboto3 15.5.0 强制 boto3>=1.40.61**——已升级并锁定
3. **`boto3` 保留**到 PR #11 收尾（sync `FileService` 仍需 boto3）——已对齐 PR #8 决策
4. **boto3 升 1.40.39 → 1.40.61** 是小版本变化，行为应一致（但需在 production 验证）
5. **aioboto3 安装时**自动升级 aiohttp / aiobotocore 等多个依赖（aiohttp 3.14.1 / aiobotocore 2.25.1），需要确认无版本冲突
6. **LSP 误报**：pyright 解析 aioboto3 内部协程链时报类型错误（如 `CoroutineType` 不能赋给 `str`），不影响运行（实际 `await` 后值类型正确）

## 验证结果

```bash
# 1. 单测全绿
python -m pytest -q --ignore=tests/e2e                  # 138 passed（原 131 + 新增 7）
python -m pytest -q tests/test_async_s3_adapter.py -v   # 7 passed
python -m pytest -q tests/test_web_messages_files.py     # 文件 web 路由未受影响
python -m pytest -q tests/test_file_cleanup.py           # sync FileService 未受影响
python -m pytest -q tests/test_api_endpoints.py          # FakeFileService 覆盖不受影响

# 2. e2e 验证
python -m pytest -q tests/e2e/test_auth.py               # 3 passed（startup lifespan 验证 sync 路径走通）
```

## 完成判据

- [x] `aioboto3==15.5.0` + `boto3==1.40.61` 加入 `requirements.txt`
- [x] `AsyncS3Adapter` 3 方法 async，方法签名与 sync 版一致
- [x] `S3Adapter`（sync）**未动**
- [x] `AsyncFileService` 4 处 `to_thread` 桥接全部删除
- [x] `tests/test_async_s3_adapter.py` 7 用例全过
- [x] `pytest -q --ignore=tests/e2e` 138 passed
- [x] e2e `tests/e2e/test_auth.py` 3 passed
- [x] `pool_runner._cleanup_expired_files` + `startup._register_file_cleanup_job` **未动**（sync 路径保留）
- [x] **业务行为 0 变化**

## 后续（PR #11 收尾）

- 删 `boto3` 依赖
- 删 sync `S3Adapter` 类
- 改 `pool_runner._cleanup_expired_files` + `startup._register_file_cleanup_job` 走 `AsyncS3Adapter` + async `FileService`
- `get_file_service` / `get_s3_adapter` / `get_async_file_service` / `get_async_s3_adapter` 统一合并
- 删 `tests/test_file_cleanup.py` 同步版测试，新建 async 版
